print('Введите предложения через пробел: ')
sentences = input().split('.') #читаем все предложения
#перебираем каждое предложение из всех
for s in sentences:
    stroka = [i for i in range(len(s))] #создаем массив размера строки
    #переписываем строку в массив, чтобы можно было менять его данные
    for i in range(len(stroka)):
        stroka[i] = s[i]
    flag = 0 #хранит количество ковычек, если кавычек больше либо равно 2, условие будет верное
    try:
       ind1 = stroka.index('"') #поиск первых кавычек
       stroka[ind1] = 0 #меняем кавычки на 0, чтобы не считать их снова
       flag = 1
    except:
        flag = 0
    try:
        ind2 = stroka.index('"')#поиск вторых кавычек
        flag = 2
    except:
        flag = 0
    if flag == 2:
        print(s)
